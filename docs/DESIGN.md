# Modern Board - 設計資料

**最終更新**: 2025-11-23

## 目次

1. [プロジェクト概要](#プロジェクト概要)
2. [機能要件](#機能要件)
3. [データモデル設計](#データモデル設計)
4. [API設計](#api設計)
5. [フロントエンド設計](#フロントエンド設計)
6. [非機能要件](#非機能要件)

---

## プロジェクト概要

### コンセプト

モダンな技術スタックで構築した匿名掲示板アプリケーション。
5ch（旧2ch）スタイルの掲示板に、リアルタイム性・ソーシャル機能・ゲーミフィケーション要素を加えた次世代型掲示板。

### 主な特徴

- 🎭 **完全匿名制**: ユーザー登録不要、セッションベースの一時ID
- ⚡ **リアルタイム性**: スレッドの勢い計算、リアルタイムアクティビティ表示
- 🎮 **ゲーミフィケーション**: レベル・ポイントシステム、MVPランキング
- 🎨 **モダンUI**: ダークモード、レスポンシブデザイン、アニメーション
- 📊 **統計・分析**: スレッド統計、トレンド分析、ユーザーアクティビティ

---

## 機能要件

### 1. スレッド管理機能

#### 1.1 スレッド一覧表示

- **要件**: カテゴリ・ソート条件に応じたスレッド一覧の表示
- **ソート条件**:
  - 勢い順（レス速度）
  - 新着順（作成日時）
  - レス数順
  - 人気順（閲覧数 + レス数 + リアクション数）
- **フィルタ条件**:
  - カテゴリ（プログラミング、雑談、相談、ニュース、質問）
  - タグ
  - 検索クエリ（タイトル・本文）
  - ピン留めスレッド優先表示
- **表示情報**:
  - スレッドID、タイトル、カテゴリ
  - レス数、参加者数、閲覧数
  - 勢い（レス/時）
  - 最終投稿日時
  - タグ一覧
  - プレビューテキスト
  - ピン留め・HOT・新着バッジ

#### 1.2 スレッド作成

- **要件**: 新規スレッドの作成
- **入力項目**:
  - タイトル（必須、最大200文字）
  - カテゴリ（必須）
  - 本文（1レス目、必須、最大10000文字）
  - タグ（任意、最大5個）
- **制約**:
  - 同一セッションからの連続投稿は5分間隔
  - タイトルの重複チェック（24時間以内）

#### 1.3 スレッド詳細表示

- **要件**: スレッド内のレス一覧と詳細情報の表示
- **表示情報**:
  - スレッドヘッダー（タイトル、統計、タグ）
  - レス一覧（番号順）
  - スレッド統計（平均レス速度、ピーク時間、ユニークID数、sage率）
- **機能**:
  - レスの無限スクロール / ページネーション
  - アンカージャンプ（>>123 クリックで該当レスへ移動）
  - 自動更新（30秒間隔、ON/OFF切替可能）

#### 1.4 スレッドブックマーク

- **要件**: スレッドをブックマークして後で参照
- **保存先**: LocalStorage（ユーザー端末）

### 2. レス（投稿）管理機能

#### 2.1 レス投稿

- **要件**: スレッドへのレス投稿
- **入力項目**:
  - 本文（必須、最大10000文字）
  - 画像添付（任意、最大5MB）
  - sage（任意）
- **機能**:
  - アンカー参照（>>番号 で他レスを参照）
  - 絵文字入力サポート
  - プレビュー機能
- **制約**:
  - 同一セッションからの連続投稿は30秒間隔
  - スパムフィルタリング

#### 2.2 レス表示

- **要件**: レスの表示
- **表示情報**:
  - レス番号
  - 投稿者名（名無しさん）
  - 投稿者ID（セッションベースの一時ID）
  - 投稿日時
  - 本文（改行・アンカー・リンクを適切にレンダリング）
  - リアクション数
  - sageバッジ
- **機能**:
  - アンカーリンクのホバープレビュー
  - 画像の展開表示

#### 2.3 リアクション

- **要件**: レスへのリアクション（いいね、fire、thinking等）
- **種類**:
  - 👍 いいね
  - 🔥 fire
  - 🤔 thinking
  - 😂 笑
  - 😮 驚き
  - 💯 完璧
  - 🎉 祝福
  - ❤️ ハート
- **表示**: 各リアクションの累計数
- **制約**: 同一セッションから同一レスへの同じリアクションは1回のみ

### 3. 匿名ユーザー機能

#### 3.1 セッションベースID

- **要件**: ユーザー登録なしでの投稿
- **実装**: セッションごとにユニークなIDを生成（例: ID:a1b2c3d4）
- **有効期限**: セッション終了まで（ブラウザクローズ）
- **表示**: 同一スレッド内では同じIDを維持

#### 3.2 レベル・ポイントシステム（フロントエンドのみ）

- **要件**: ユーザーのアクティビティを可視化
- **獲得条件**:
  - スレッド作成: +50pt
  - レス投稿: +10pt
  - リアクション獲得: +5pt
- **レベル**: ポイント累計でレベルアップ
- **表示**: サイドバーのマイステータス
- **保存先**: LocalStorage

#### 3.3 統計情報

- **要件**: ユーザーのアクティビティ統計
- **表示項目**:
  - 今日の投稿数
  - 獲得いいね数
  - 次のレベルまでのポイント

### 4. カテゴリ・タグ機能

#### 4.1 カテゴリ

- **固定カテゴリ**:
  - プログラミング
  - 雑談
  - 相談
  - ニュース
  - 質問
- **機能**: カテゴリでのフィルタリング

#### 4.2 タグ

- **要件**: スレッドへのタグ付け
- **表示**: スレッド一覧・詳細にタグを表示
- **機能**: タグでの検索・フィルタリング

### 5. 検索・フィルタリング機能

#### 5.1 全文検索

- **要件**: スレッドタイトル・本文の全文検索
- **検索対象**: タイトル、プレビューテキスト、タグ
- **結果**: マッチしたスレッド一覧

#### 5.2 ソート機能

- **要件**: 様々な条件でスレッドをソート
- **ソート条件**: 勢い順、新着順、レス数順、人気順

### 6. リアルタイム機能

#### 6.1 勢い計算

- **要件**: スレッドの活発度を数値化
- **計算式**: 勢い = 直近1時間のレス数 × 24
- **更新頻度**: 5分ごとに再計算
- **表示**: スレッド一覧に勢い値を表示

#### 6.2 リアルタイムアクティビティ

- **要件**: 最新の投稿・スレッド作成をリアルタイム表示
- **表示場所**: サイドバー
- **更新頻度**: 30秒ごと

#### 6.3 自動更新

- **要件**: スレッド詳細画面の自動更新
- **更新内容**: 新着レスの追加表示
- **更新頻度**: 30秒ごと（設定でON/OFF可能）
- **通知**: 新着レスがある場合はバッジ表示

### 7. トレンド・統計機能

#### 7.1 トレンドワード

- **要件**: よく使われている単語のランキング
- **更新頻度**: 1時間ごと
- **表示**: サイドバーにTOP5表示

#### 7.2 MVPユーザー

- **要件**: 今週最もアクティブなユーザーのランキング
- **ランキング基準**: 投稿数 + 獲得いいね数
- **表示**: サイドバーにTOP3表示

#### 7.3 殿堂入りスレッド

- **要件**: 1000レス達成など、記念碑的なスレッドの表示
- **条件**: 1000レス達成、10000閲覧達成など

#### 7.4 サイト統計

- **要件**: サイト全体の統計情報
- **表示項目**:
  - 新規スレッド数（今日）
  - 総レス数（今日）
  - ユニークユーザー数（今日）
  - アクティブスレッド数

### 8. UI/UX機能

#### 8.1 ダークモード

- **要件**: ライト/ダークテーマの切り替え
- **保存先**: LocalStorage

#### 8.2 レスポンシブデザイン

- **要件**: PC・タブレット・スマートフォンでの最適表示
- **ブレークポイント**:
  - モバイル: ~640px
  - タブレット: 641px~1024px
  - デスクトップ: 1025px~

#### 8.3 モバイル最適化

- **要件**: モバイル専用UI
- **機能**:
  - ドロワーサイドバー
  - 固定ボトムナビゲーション
  - スワイプジェスチャー

#### 8.4 通知

- **要件**: ユーザーアクションへの通知
- **通知タイプ**:
  - 新着レス
  - リアクション獲得
  - メンテナンス情報
- **表示**: ヘッダーの通知アイコン（バッジ付き）

---

## データモデル設計

### 1. Category（カテゴリ）

```python
class Category(models.Model):
    """スレッドのカテゴリ"""
    name = CharField(max_length=50, unique=True)  # カテゴリ名
    slug = SlugField(max_length=50, unique=True)  # URLスラッグ
    description = TextField(blank=True)           # 説明
    icon = CharField(max_length=50, blank=True)  # アイコン（絵文字）
    order = IntegerField(default=0)              # 表示順序
    is_active = BooleanField(default=True)       # 有効/無効
    created_at = DateTimeField(auto_now_add=True)

    class Meta:
        ordering = ['order', 'name']
```

### 2. Tag（タグ）

```python
class Tag(models.Model):
    """スレッド・レスのタグ"""
    name = CharField(max_length=30, unique=True)  # タグ名
    slug = SlugField(max_length=30, unique=True)  # URLスラッグ
    usage_count = IntegerField(default=0)        # 使用回数
    created_at = DateTimeField(auto_now_add=True)

    class Meta:
        ordering = ['-usage_count', 'name']
```

### 3. Thread（スレッド）

```python
class Thread(models.Model):
    """掲示板のスレッド"""
    title = CharField(max_length=200)            # タイトル
    category = ForeignKey(Category)              # カテゴリ
    tags = ManyToManyField(Tag, blank=True)     # タグ

    # 統計情報
    post_count = IntegerField(default=0)        # レス数
    view_count = IntegerField(default=0)        # 閲覧数
    participant_count = IntegerField(default=0) # 参加者数
    reaction_count = IntegerField(default=0)    # リアクション総数
    momentum = FloatField(default=0.0)          # 勢い

    # ステータス
    is_pinned = BooleanField(default=False)     # ピン留め
    is_hot = BooleanField(default=False)        # HOT判定
    is_locked = BooleanField(default=False)     # ロック（書き込み不可）
    is_archived = BooleanField(default=False)   # アーカイブ済み

    # 日時
    created_at = DateTimeField(auto_now_add=True)      # 作成日時
    updated_at = DateTimeField(auto_now=True)          # 更新日時
    last_post_at = DateTimeField(null=True, blank=True) # 最終投稿日時

    # メタ情報
    first_post_preview = TextField(blank=True)  # 1レス目のプレビュー
    author_session = CharField(max_length=64)   # 作成者のセッションID

    class Meta:
        ordering = ['-is_pinned', '-momentum', '-last_post_at']
        indexes = [
            Index(fields=['-momentum']),
            Index(fields=['-last_post_at']),
            Index(fields=['category', '-momentum']),
        ]
```

### 4. Post（レス）

```python
class Post(models.Model):
    """スレッド内のレス（投稿）"""
    thread = ForeignKey(Thread, related_name='posts') # スレッド
    number = IntegerField()                       # レス番号（スレッド内）

    # 投稿内容
    content = TextField(max_length=10000)        # 本文
    author_name = CharField(max_length=50)       # 投稿者名（デフォルト: 名無しさん）
    author_session = CharField(max_length=64)    # 投稿者セッションID
    author_id_hash = CharField(max_length=16)    # 表示用ID（例: ID:a1b2c3d4）

    # オプション
    is_sage = BooleanField(default=False)        # sage
    image = ImageField(blank=True, null=True)    # 添付画像

    # 統計
    reaction_count = IntegerField(default=0)     # リアクション総数

    # 日時
    created_at = DateTimeField(auto_now_add=True)
    updated_at = DateTimeField(auto_now=True)
    ip_address = GenericIPAddressField()         # IPアドレス（管理用）

    class Meta:
        ordering = ['thread', 'number']
        unique_together = [['thread', 'number']]
        indexes = [
            Index(fields=['thread', 'number']),
            Index(fields=['-created_at']),
        ]
```

### 5. Reaction（リアクション）

```python
class Reaction(models.Model):
    """レスへのリアクション"""
    REACTION_TYPES = [
        ('like', '👍 いいね'),
        ('fire', '🔥 Fire'),
        ('thinking', '🤔 考え中'),
        ('laugh', '😂 笑'),
        ('surprise', '😮 驚き'),
        ('perfect', '💯 完璧'),
        ('celebrate', '🎉 祝福'),
        ('heart', '❤️ ハート'),
    ]

    post = ForeignKey(Post, related_name='reactions') # レス
    reaction_type = CharField(max_length=20, choices=REACTION_TYPES)
    session_id = CharField(max_length=64)        # リアクションしたセッションID
    created_at = DateTimeField(auto_now_add=True)

    class Meta:
        unique_together = [['post', 'reaction_type', 'session_id']]
        indexes = [
            Index(fields=['post', 'reaction_type']),
        ]
```

### 6. UserSession（ユーザーセッション）

```python
class UserSession(models.Model):
    """匿名ユーザーのセッション情報"""
    session_key = CharField(max_length=64, unique=True) # セッションキー

    # 統計（フロントエンド表示用）
    thread_count = IntegerField(default=0)       # 作成スレッド数
    post_count = IntegerField(default=0)         # 投稿レス数
    reaction_received_count = IntegerField(default=0) # 獲得リアクション数

    # 制限
    last_thread_at = DateTimeField(null=True)    # 最後のスレッド作成日時
    last_post_at = DateTimeField(null=True)      # 最後のレス投稿日時

    # 日時
    first_seen_at = DateTimeField(auto_now_add=True)  # 初回アクセス
    last_seen_at = DateTimeField(auto_now=True)       # 最終アクセス

    class Meta:
        ordering = ['-last_seen_at']
```

---

## API設計

### ベースURL

```
/api/v1/
```

### エンドポイント一覧

#### 1. スレッド関連

| Method | Endpoint | 説明 |
|--------|----------|------|
| GET | `/threads/` | スレッド一覧取得 |
| POST | `/threads/` | スレッド作成 |
| GET | `/threads/{id}/` | スレッド詳細取得 |
| PATCH | `/threads/{id}/` | スレッド更新（管理者のみ） |
| DELETE | `/threads/{id}/` | スレッド削除（管理者のみ） |
| GET | `/threads/{id}/posts/` | スレッド内のレス一覧 |
| POST | `/threads/{id}/posts/` | レス投稿 |

#### 2. レス関連

| Method | Endpoint | 説明 |
|--------|----------|------|
| GET | `/posts/{id}/` | レス詳細取得 |
| POST | `/posts/{id}/reactions/` | リアクション追加 |
| DELETE | `/posts/{id}/reactions/{type}/` | リアクション削除 |

#### 3. カテゴリ・タグ

| Method | Endpoint | 説明 |
|--------|----------|------|
| GET | `/categories/` | カテゴリ一覧 |
| GET | `/tags/` | タグ一覧 |
| GET | `/tags/trending/` | トレンドタグ |

#### 4. 統計・その他

| Method | Endpoint | 説明 |
|--------|----------|------|
| GET | `/stats/site/` | サイト統計 |
| GET | `/stats/trending/` | トレンド情報 |
| GET | `/activity/recent/` | 最新アクティビティ |

### 詳細仕様

#### GET /threads/

**クエリパラメータ**:
- `category`: カテゴリでフィルタ（slug）
- `tags`: タグでフィルタ（カンマ区切り）
- `search`: 全文検索
- `ordering`: ソート（momentum, -created_at, -post_count, -view_count）
- `page`: ページ番号
- `page_size`: 1ページあたりの件数（デフォルト: 20）

**レスポンス例**:
```json
{
  "count": 150,
  "next": "http://localhost:8000/api/v1/threads/?page=2",
  "previous": null,
  "results": [
    {
      "id": 1,
      "title": "【速報】React v19がついにリリース！",
      "category": {
        "id": 1,
        "name": "プログラミング",
        "slug": "programming"
      },
      "tags": ["React", "フロントエンド", "速報"],
      "post_count": 856,
      "view_count": 15234,
      "participant_count": 234,
      "reaction_count": 1245,
      "momentum": 125.5,
      "is_pinned": true,
      "is_hot": true,
      "first_post_preview": "マジでヤバい新機能きたぞ！！",
      "last_post_at": "2025-01-15T14:28:00Z",
      "created_at": "2025-01-15T10:00:00Z"
    }
  ]
}
```

#### POST /threads/

**リクエストボディ**:
```json
{
  "title": "お前らが一番衝撃を受けたバグ書いてけ",
  "category": "programming",
  "content": "俺は本番環境でrm -rf /やっちまった時だな...",
  "tags": ["バグ", "プログラミング"]
}
```

**レスポンス**:
```json
{
  "id": 42,
  "title": "お前らが一番衝撃を受けたバグ書いてけ",
  "category": {
    "id": 1,
    "name": "プログラミング",
    "slug": "programming"
  },
  "tags": ["バグ", "プログラミング"],
  "post_count": 1,
  "created_at": "2025-01-15T14:30:00Z"
}
```

---

## フロントエンド設計

### ディレクトリ構造

```
frontend/src/
├── components/          # Reactコンポーネント
│   ├── thread/
│   │   ├── ThreadList.tsx
│   │   ├── ThreadCard.tsx
│   │   ├── ThreadDetail.tsx
│   │   └── ThreadForm.tsx
│   ├── post/
│   │   ├── PostList.tsx
│   │   ├── PostItem.tsx
│   │   └── PostForm.tsx
│   ├── sidebar/
│   │   ├── Sidebar.tsx
│   │   ├── UserStatus.tsx
│   │   ├── TrendingWords.tsx
│   │   └── SiteStats.tsx
│   ├── common/
│   │   ├── Header.tsx
│   │   ├── Navigation.tsx
│   │   └── SearchBar.tsx
│   └── layout/
│       ├── MainLayout.tsx
│       └── MobileLayout.tsx
├── services/            # API通信サービス
│   ├── api.ts          # Axiosインスタンス
│   ├── threadService.ts
│   ├── postService.ts
│   └── statsService.ts
├── hooks/              # カスタムフック
│   ├── useThread.ts
│   ├── usePost.ts
│   ├── useSession.ts
│   └── useLocalStorage.ts
├── types/              # TypeScript型定義
│   └── index.ts
├── utils/              # ユーティリティ関数
│   ├── date.ts
│   ├── anchor.ts
│   └── session.ts
└── App.tsx
```

### 主要コンポーネント

1. **ThreadList**: スレッド一覧表示
2. **ThreadDetail**: スレッド詳細・レス表示
3. **PostForm**: レス投稿フォーム
4. **Sidebar**: サイドバー（統計・トレンド）
5. **Header**: ヘッダー（検索・通知）

---

## 非機能要件

### パフォーマンス

- **レスポンスタイム**: API応答時間 < 200ms
- **ページロード**: First Contentful Paint < 1.5s
- **同時接続**: 1000ユーザー対応

### セキュリティ

- **CORS**: フロントエンドからのアクセスのみ許可
- **レート制限**: IP/セッションあたりの投稿制限
- **XSS対策**: ユーザー入力のサニタイゼーション
- **スパム対策**: 連続投稿の制限

### 可用性

- **稼働率**: 99.9%以上
- **バックアップ**: 日次自動バックアップ

### スケーラビリティ

- **水平スケーリング**: アプリケーションサーバーの追加対応
- **キャッシング**: Redis利用（統計情報、トレンドデータ）
